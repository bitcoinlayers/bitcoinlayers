import { LayerProject, Type, LiveStatus, Purpose, RiskFactor, EntityType, Site, RiskCategory, RiskSection, ContentSection, AssessmentCategory, AssessmentSection } from "@/content/props";

const botanix: LayerProject = [object Object];

export default botanix;